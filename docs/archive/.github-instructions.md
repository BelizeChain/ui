# BelizeChain UI Development Instructions

## 🎨 Design System (January 2026 Update)

**ALL** BelizeChain UIs now follow the **modern dark theme glass morphism design** from Maya Wallet.

### Core Principles

1. **Dark-First Design**: All interfaces use dark backgrounds (gray-900 → gray-800 gradients)
2. **Glass Morphism**: GlassCard component with backdrop-blur for depth
3. **Consistent Components**: All UIs share components from `@belizechain/shared`
4. **Emerald Accents**: Primary actions use emerald-500/emerald-400
5. **Typography**: White/Gray-300 for headings, Gray-400 for descriptions

## 📁 Project Structure

```
ui/
├── maya-wallet/           # ✅ REFERENCE IMPLEMENTATION - Citizen/business wallet
├── blue-hole-portal/      # 🔄 Government dashboard (needs update)
├── nft-marketplace/       # 🔄 NFT trading platform (needs update)
├── shared/               # ✅ Modern component library (updated from Maya Wallet)
└── .github-instructions.md  # This file
```

## 🚀 Component Library (@belizechain/shared)

### Primary Component: GlassCard

**Standard Pattern for All Pages:**

```tsx
'use client';
import { useRouter } from 'next/navigation';
import { ArrowLeft, PageIcon } from 'phosphor-react';
import { GlassCard } from '@belizechain/shared';

export default function PageName() {
  const router = useRouter();
  
  return (
    <div className="min-h-screen bg-gradient-to-b from-gray-900 via-gray-800 to-gray-900 pb-24">
      {/* Sticky Header with Back Button */}
      <div className="sticky top-0 bg-gray-900/80 backdrop-blur-xl px-6 py-4 z-10 border-b border-gray-700/50">
        <div className="flex items-center justify-between p-4">
          <div className="flex items-center gap-3">
            <button onClick={() => router.back()} className="p-2 hover:bg-gray-800 rounded-full transition-colors">
              <ArrowLeft size={24} className="text-gray-300" weight="bold" />
            </button>
            <div>
              <h1 className="text-xl font-bold text-white">Page Title</h1>
              <p className="text-xs text-gray-400">Subtitle</p>
            </div>
          </div>
          <PageIcon size={32} className="text-emerald-400" weight="duotone" />
        </div>
      </div>

      {/* Main Content Container */}
      <div className="p-4 space-y-6">
        <GlassCard variant="dark-medium" blur="lg" className="p-6">
          {/* Content here */}
        </GlassCard>
      </div>
    </div>
  );
}
```

### GlassCard Variants

| Variant | Use Case | Background |
|---------|----------|------------|
| `dark-light` | Subtle containers | `bg-gray-800/40` |
| `dark-medium` | **Standard pages** (DEFAULT) | `bg-gray-800/70` |
| `dark` | Strong emphasis | `bg-gray-800/60` |
| `gradient` | Primary CTAs | `emerald-to-teal` |

### Specialized Cards

```tsx
// Asset display
<AssetCard
  name="DALLA"
  symbol="DALLA"
  balance="1,250.00"
  value="$1,250.00"
  change="+5.2%"
  icon={<Coins size={32} className="text-emerald-400" weight="duotone" />}
  variant="emerald"
/>

// Statistics
<StatCard
  title="Total Staked"
  value="5,000 DALLA"
  change="+12.5%"
  icon={<ChartLine size={24} className="text-emerald-400" weight="duotone" />}
/>

// Community posts
<PostCard
  author="John Doe"
  content="Great project!"
  timestamp="2h ago"
  likes={42}
/>

// Governance proposals
<ProposalCard
  title="Treasury Allocation"
  status="active"
  votesFor={1250}
  votesAgainst={320}
/>
```

## 🎨 Color System

### Backgrounds
- **Page Background**: `bg-gradient-to-b from-gray-900 via-gray-800 to-gray-900`
- **Headers**: `bg-gray-900/80 backdrop-blur-xl`
- **Cards**: `bg-gray-800/70` (via GlassCard variant="dark-medium")
- **Stats**: `bg-gray-800/30 backdrop-blur-sm`

### Text Colors
- **Headings**: `text-white`
- **Subheadings**: `text-gray-300`
- **Descriptions**: `text-gray-400`
- **Muted**: `text-gray-500`

### Accent Colors
| Color | Use Case | Classes |
|-------|----------|---------|
| Emerald | Primary actions, positive | `text-emerald-400`, `bg-emerald-500` |
| Orange | Warnings, land ledger | `text-orange-400`, `bg-orange-900/20` |
| Purple | Quantum, premium | `text-purple-400`, `bg-purple-900/20` |
| Blue | Links, bridges | `text-blue-400`, `bg-blue-900/20` |
| Red | Errors, security | `text-red-400`, `bg-red-900/20` |

### Borders
- **Standard**: `border-gray-700/30`
- **Medium**: `border-gray-700/40`
- **Strong**: `border-gray-700/50`

## 🔧 Radix UI Components

All UIs should use Radix UI primitives from shared library:

```tsx
import { 
  Tabs, TabsList, TabsTrigger, TabsContent,
  Progress,
  Dialog, DialogContent, DialogHeader,
  Avatar, AvatarImage, AvatarFallback
} from '@belizechain/shared';
```

### Tab Switcher Pattern

```tsx
<GlassCard variant="dark-medium" blur="lg" className="p-1">
  <Tabs value={activeTab} onValueChange={setActiveTab}>
    <TabsList className="grid grid-cols-3 gap-1 bg-transparent">
      <TabsTrigger value="tab1">Tab 1</TabsTrigger>
      <TabsTrigger value="tab2">Tab 2</TabsTrigger>
      <TabsTrigger value="tab3">Tab 3</TabsTrigger>
    </TabsList>
    
    <TabsContent value="tab1">{/* Content */}</TabsContent>
    <TabsContent value="tab2">{/* Content */}</TabsContent>
    <TabsContent value="tab3">{/* Content */}</TabsContent>
  </Tabs>
</GlassCard>
```

## 🎯 Icon System (Phosphor Icons)

### Icon Weights
- **Headers/CTAs**: `weight="duotone"` (NEW standard)
- **Navigation**: `weight="bold"`
- **UI elements**: `weight="regular"`
- **Inline icons**: `weight="fill"` (for filled circles, badges)

### Icon Sizes
- **Page headers**: `size={32}`
- **Section headers**: `size={24}`
- **Buttons**: `size={20}`
- **Inline**: `size={16}`

### Common Icons
```tsx
import {
  ArrowLeft,        // Back navigation
  Coins,           // DALLA token
  ChartLine,       // Analytics
  ShieldCheck,     // Security
  Users,           // Community
  Lightning,       // Quantum
  Database,        // Storage
  GitBranch,       // Bridges
  GlobeHemisphereWest, // BNS
  FileText,        // Documents
} from 'phosphor-react';
```

## 📱 Responsive Design

### Breakpoints (Tailwind)
- **Mobile**: Default (< 640px)
- **Tablet**: `md:` (768px+)
- **Desktop**: `lg:` (1024px+)
- **Wide**: `xl:` (1280px+)

### Mobile-First Patterns
```tsx
// Stack on mobile, grid on desktop
<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">

// Hide on mobile, show on tablet+
<div className="hidden md:block">

// Full width on mobile, constrained on desktop
<div className="w-full md:w-1/2 lg:w-1/3">
```

## 🚫 What NOT to Do

### ❌ AVOID Old Light Theme Components
```tsx
// ❌ WRONG - Old Card component
import { Card } from '@belizechain/shared';
<Card className="bg-white shadow-md">

// ✅ CORRECT - New GlassCard
import { GlassCard } from '@belizechain/shared';
<GlassCard variant="dark-medium" blur="lg">
```

### ❌ AVOID Old Color Palette
```tsx
// ❌ WRONG - Old Belize cultural colors
className="bg-caribbean-500 text-white"
className="text-bluehole-900"
className="bg-maya-600"

// ✅ CORRECT - Modern dark theme
className="bg-emerald-500 text-white"
className="text-white"
className="bg-gray-800/70"
```

### ❌ AVOID Custom Gradient Backgrounds on Cards
```tsx
// ❌ WRONG - Custom gradient
<div className="bg-gradient-to-br from-gray-800 to-gray-900 rounded-2xl border">

// ✅ CORRECT - GlassCard component
<GlassCard variant="dark-medium" blur="lg">
```

### ❌ AVOID Inline Styles
```tsx
// ❌ WRONG
<div style={{ backgroundColor: 'gray', padding: '16px' }}>

// ✅ CORRECT
<GlassCard variant="dark-medium" blur="lg" className="p-4">
```

## 📋 Component Checklist

Before creating a new UI component, verify:

- [ ] Uses GlassCard for containers
- [ ] Sticky header with back button (if detail page)
- [ ] Dark gradient background (`from-gray-900 via-gray-800 to-gray-900`)
- [ ] Icons use `weight="duotone"` for modern look
- [ ] Text colors: `text-white`, `text-gray-300`, `text-gray-400`
- [ ] Tab switchers wrapped in GlassCard with `p-1`
- [ ] Borders: `border-gray-700/30` to `border-gray-700/50`
- [ ] Responsive design with mobile-first approach
- [ ] Imports from `@belizechain/shared`
- [ ] No old Card/Button components

## 🔄 Migration Priority

1. ✅ **Maya Wallet** - COMPLETE (reference implementation)
2. ✅ **Shared Library** - UPDATED (modern components copied from Maya Wallet)
3. 🔄 **Blue Hole Portal** - NEXT (government dashboard needs full modernization)
4. 🔄 **NFT Marketplace** - PENDING (low priority)

## 🏗️ Development Workflow

### 1. Install Dependencies
```bash
cd ui
npm install
```

### 2. Start Development Server
```bash
# Individual app
cd maya-wallet && npm run dev

# Or all apps (from ui/ root)
npm run dev:all
```

### 3. Build Shared Library
```bash
cd shared
npm run build
```

### 4. Type Checking
```bash
npm run type-check
```

## 📝 File Naming Conventions

- **Pages**: `kebab-case.tsx` (e.g., `land-ledger.tsx`)
- **Components**: `PascalCase.tsx` (e.g., `GlassCard.tsx`)
- **Hooks**: `camelCase.ts` with `use` prefix (e.g., `useWallet.ts`)
- **Utils**: `kebab-case.ts` (e.g., `format-currency.ts`)
- **Types**: `PascalCase.ts` or `types.ts`

## 🎓 Learning Resources

- **Reference Implementation**: `/ui/maya-wallet/src/app/` (all pages follow modern pattern)
- **Component Examples**: `/ui/maya-wallet/src/components/ui/`
- **Shared Library**: `/ui/shared/README.md`
- **Phosphor Icons**: https://phosphoricons.com
- **Radix UI**: https://www.radix-ui.com/docs/primitives
- **Tailwind CSS**: https://tailwindcss.com/docs

---

**Last Updated**: January 25, 2026  
**Design System Version**: 1.0 (Modern Dark Theme)  
**Reference**: Maya Wallet (Complete)
